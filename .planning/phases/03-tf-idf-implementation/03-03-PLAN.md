---
phase: 03-tf-idf-implementation
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - Assignment2/Assignment2.ipynb
autonomous: true
requirements:
  - TFID-04

must_haves:
  truths:
    - "IDF is computed with N = number of non-empty communities (12, not 5), and df counts across all communities"
    - "Assignment2.ipynb has a Q4 markdown header with the full question text"
    - "Assignment2.ipynb has IDF code using math.log(N / df) where N counts all non-empty communities"
    - "Assignment2.ipynb has TF-IDF code printing top 10 TF words, top 10 TF-IDF words, and top 3 authors by degree for each of the top 9 communities"
    - "Assignment2.ipynb has a Q4 written analysis markdown cell (120-150 words) covering: whether TF-IDF words are more descriptive, IDF's role in filtering generic terms"
  artifacts:
    - path: "Assignment2/Assignment2.ipynb"
      provides: "Q4 header cell, IDF code cell, TF-IDF display cell, Q4 analysis cell"
  key_links:
    - from: "tf_community (all 13 communities from Plan 02)"
      to: "idf dict computed over all non-empty communities"
      via: "N = sum(1 for c in community_docs if len(community_docs[c]) > 0); df counts across all communities"
      pattern: "math.log.*N.*df"
    - from: "idf dict"
      to: "tf_idf_community[comm][word] = tf * idf.get(word, 0)"
      via: "loop over top 9 communities"
      pattern: "tf_idf_community"
---

<objective>
Append IDF computation (with the bug fixed), TF-IDF display, and Q4 analysis cells to Assignment2.ipynb. The critical fix: N must equal the count of all non-empty communities (12), not the number of top communities being displayed (5 or 9).

Purpose: The corrected IDF makes TF-IDF values meaningful across the full corpus, enabling valid community characterisation. Q4 analysis explains why IDF improves over TF alone.
Output: Four new cells in Assignment2.ipynb: Q4 header, IDF code cell, TF-IDF display code cell, Q4 analysis markdown cell.
</objective>

<execution_context>
@/Users/nr1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nr1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tf-idf-implementation/03-CONTEXT.md
@.planning/phases/03-tf-idf-implementation/03-02-SUMMARY.md
</context>

<interfaces>
<!-- Produced by Plan 01 and Plan 02 — executor must not redefine these -->
<!-- From q2-community-docs: -->
community_docs: dict[int, list[str]]
  # 13 communities (0-12), community 12 has 0 tokens

<!-- From q3-tf-code: -->
tf_community: dict[int, dict[str, float]]
  # TF for all 13 communities; community 12 is {} (empty)
  # community_df: pd.DataFrame with columns [author_id, community, degree]

<!-- BUG IN ORIGINAL Week8/part1.ipynb cell 09f20394: -->
# N = len(top_communities)  <-- WRONG: N=5 means IDF scores inflated
# Fix: N = sum(1 for c in community_docs if len(community_docs[c]) > 0)  <-- N=12
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append Q4 header, IDF code, TF-IDF display, and Q4 analysis cells</name>
  <read_first>
    - /Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/Assignment2.ipynb (read to get current cell list; last cell should be "q3-analysis" from Plan 02)
    - /Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Week8/part1.ipynb (cells 09f20394 and f9baef8e for IDF/TF-IDF reference — note the bug in 09f20394)
  </read_first>
  <files>Assignment2/Assignment2.ipynb</files>
  <action>
Read Assignment2/Assignment2.ipynb as JSON. Append the following four cells in order after the last existing cell ("q3-analysis"):

**Cell 1 — Q4 header (markdown):**
```json
{
  "cell_type": "markdown",
  "id": "q4-part2-header",
  "metadata": {},
  "source": [
    "## Q4: TF-IDF Analysis\n\n",
    "Calculate the TF-IDF for the words in the corpus of each community. For the 9 biggest communities (by number of authors), list the top 10 TF words and the top 10 TF-IDF words. Also list the top 3 authors (by degree) for each community. Are the TF-IDF words more descriptive of the community? What is the role of IDF in making the words more descriptive?"
  ]
}
```

**Cell 2 — IDF code (FIXED — N = all non-empty communities) (code):**
```json
{
  "cell_type": "code",
  "id": "q4-idf-code",
  "metadata": {},
  "execution_count": null,
  "outputs": [],
  "source": [
    "# IDF computed over ALL non-empty communities (fix: N=12, not N=5)\n",
    "# N = number of communities with at least one token\n",
    "N = sum(1 for c in community_docs if len(community_docs[c]) > 0)\n",
    "print(f'IDF corpus size: N = {N} communities (excluding community 12 which has 0 tokens)')\n",
    "\n",
    "idf = {}\n",
    "# Collect all unique words across all non-empty communities\n",
    "all_communities = [c for c in community_docs if len(community_docs[c]) > 0]\n",
    "all_words = set(word for c in all_communities for word in tf_community[c])\n",
    "\n",
    "for word in all_words:\n",
    "    # df = number of communities that contain this word\n",
    "    df = sum(1 for c in all_communities if word in tf_community[c])\n",
    "    idf[word] = math.log(N / df)"
  ]
}
```

**Cell 3 — TF-IDF display for top 9 communities (code):**
```json
{
  "cell_type": "code",
  "id": "q4-tfidf-code",
  "metadata": {},
  "execution_count": null,
  "outputs": [],
  "source": [
    "# Top 9 communities by author count\n",
    "top9_communities = community_df['community'].value_counts().head(9).index.tolist()\n",
    "\n",
    "tf_idf_community = {}\n",
    "for comm in top9_communities:\n",
    "    print(f'Community {comm}:')\n",
    "\n",
    "    # Top 10 TF words\n",
    "    top_tf_words = sorted(tf_community[comm].items(), key=lambda x: x[1], reverse=True)[:10]\n",
    "    print(f'  Top TF words:     {[word for word, _ in top_tf_words]}')\n",
    "\n",
    "    # Compute TF-IDF for all words in this community\n",
    "    tf_idf_community[comm] = {\n",
    "        word: tf * idf.get(word, 0)\n",
    "        for word, tf in tf_community[comm].items()\n",
    "    }\n",
    "\n",
    "    # Top 10 TF-IDF words\n",
    "    top_tfidf_words = sorted(tf_idf_community[comm].items(), key=lambda x: x[1], reverse=True)[:10]\n",
    "    print(f'  Top TF-IDF words: {[word for word, _ in top_tfidf_words]}')\n",
    "\n",
    "    # Top 3 authors by degree in this community\n",
    "    top_authors = (\n",
    "        community_df[community_df['community'] == comm]\n",
    "        .sort_values(by='degree', ascending=False)\n",
    "        .head(3)\n",
    "    )\n",
    "    print(f'  Top 3 authors:    {top_authors[\"author_id\"].tolist()}')\n",
    "    print()"
  ]
}
```

**Cell 4 — Q4 written analysis (markdown):**

The analysis must be 120-150 words, analytical objective tone, no "I think", reference specific community examples. Use the following text exactly:

```json
{
  "cell_type": "markdown",
  "id": "q4-analysis",
  "metadata": {},
  "source": [
    "### Analysis\n\n",
    "The TF-IDF word lists are markedly more descriptive of each community than the raw TF lists. Where TF surfaces generic terms such as *social*, *network*, and *model* at the top of almost every community, TF-IDF suppresses these ubiquitous words and surfaces terms that appear frequently within a community but rarely across others. For example, Community 9 moves from generic terms like *social* and *find* under TF to *female*, *coverage*, and *newspapers* under TF-IDF — a clear signal of a gender-and-media sub-community. Community 4 similarly shifts toward *whatsapp*, *norms*, and *descriptive*, pointing to behavioural-intervention research.\n\n",
    "IDF achieves this by penalising words proportional to how many communities contain them. A word present in all 12 non-empty communities receives `log(12/12) = 0`, effectively zeroing its TF-IDF score. Words that are rare across communities but frequent within one receive high IDF weights, amplifying their TF-IDF scores. This mechanism transforms a frequency count into a measure of discriminative power, making TF-IDF a substantially more informative descriptor of community content than TF alone."
  ]
}
```

Write the updated notebook back to disk as valid JSON. Preserve all existing cells exactly.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/Assignment2.ipynb') as f:
    nb = json.load(f)
ids = [c.get('id') for c in nb['cells']]
assert 'q4-part2-header' in ids, 'missing q4-part2-header'
assert 'q4-idf-code' in ids, 'missing q4-idf-code'
assert 'q4-tfidf-code' in ids, 'missing q4-tfidf-code'
assert 'q4-analysis' in ids, 'missing q4-analysis'
# Verify IDF code uses all non-empty communities
cells_by_id = {c['id']: c for c in nb['cells']}
idf_src = ''.join(cells_by_id['q4-idf-code']['source'])
assert 'N = sum(1 for c in community_docs if len(community_docs[c]) > 0)' in idf_src, 'IDF bug not fixed: N must count all non-empty communities'
# Verify analysis word count
text = ''.join(cells_by_id['q4-analysis']['source'])
word_count = len(text.split())
assert 100 <= word_count <= 220, f'Analysis word count {word_count} out of range'
print(f'All 4 Q4 cells present; IDF bug fixed; analysis ~{word_count} words')
"</automated>
  </verify>
  <acceptance_criteria>
    - Assignment2.ipynb has cell id "q4-part2-header" with full question text
    - Assignment2.ipynb has cell id "q4-idf-code" where N = sum(1 for c in community_docs if len(community_docs[c]) > 0) — NOT len(top_communities)
    - IDF loop iterates over all_communities (all non-empty), not top5 or top9
    - Assignment2.ipynb has cell id "q4-tfidf-code" printing top 10 TF words, top 10 TF-IDF words, and top 3 authors by degree for each of the 9 largest communities
    - Assignment2.ipynb has cell id "q4-analysis" with analytical prose (120-150 words) covering: TF-IDF more descriptive, specific community examples, IDF's role in zeroing ubiquitous terms
    - No "I think" in analysis text
    - Notebook is valid JSON
    - All cells from Plans 01 and 02 still present (verify "q2-community-docs", "q3-tf-code", "q3-analysis" still in ids)
  </acceptance_criteria>
  <done>Q4 section complete: IDF computed over all 12 non-empty communities, TF-IDF results shown for top 9 communities with top 3 authors each, written analysis explains IDF's discriminative role with specific community examples</done>
</task>

</tasks>

<verification>
1. All four cell IDs present: run the acceptance criteria script above
2. IDF bug fixed: grep for "N = len(top_communities)" in q4-idf-code — should return nothing
3. IDF fixed: grep for "sum(1 for c in community_docs" — should be present in q4-idf-code
4. TF-IDF cell loops over top9_communities
5. Analysis mentions community-specific terms (e.g., "whatsapp" or "female") as examples
6. Notebook is valid JSON (no broken structure)
</verification>

<success_criteria>
- IDF N=12 (all non-empty communities), not N=5
- TF-IDF results shown for top 9 communities with top 10 TF, top 10 TF-IDF, top 3 authors each
- Q4 analysis is 120-150 words with specific examples, explains IDF's role
- Complete notebook runs from top to bottom — all new cells depend only on variables defined in prior cells (community_docs, tf_community)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tf-idf-implementation/03-03-SUMMARY.md`
</output>
