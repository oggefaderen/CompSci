---
phase: 04-part-2-writing-and-word-clouds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assignment2/Assignment2.ipynb
autonomous: true
requirements:
  - TFID-01
  - VIZN-01

must_haves:
  truths:
    - "A markdown cell explains TF-IDF in 120-150 words with both TF and IDF defined, natural log mentioned"
    - "A code cell renders a word cloud for each of the top 9 communities, displaying real author display_names (not OpenAlex IDs) in the title"
  artifacts:
    - path: "Assignment2/Assignment2.ipynb"
      provides: "Ex1 Q1 explanation cell + Ex2 word cloud code cell"
      contains: "TF-IDF in own words"
  key_links:
    - from: "word cloud code cell"
      to: "authors_df display_name"
      via: "dict(zip(authors_df['id'], authors_df['display_name']))"
      pattern: "name_map"
---

<objective>
Append two cells to Assignment2.ipynb: (1) a markdown explanation of TF-IDF (Ex1 Q1), and (2) a code cell that generates word clouds for the top 9 communities with real author names resolved via final_authors.csv.

Purpose: Satisfies TFID-01 (explain TF-IDF) and VIZN-01 (word clouds with top 3 author names).
Output: Two new cells at the end of Assignment2.ipynb — one markdown, one code.
</objective>

<execution_context>
@/Users/nr1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nr1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-part-2-writing-and-word-clouds/04-CONTEXT.md
@.planning/phases/03-tf-idf-implementation/03-03-SUMMARY.md

<interfaces>
<!-- Variables available in Assignment2.ipynb scope from prior phases -->
<!-- authors_df is already loaded in the Part 1 foundation cell (cell a6b7c8d9) -->

From Assignment2/Assignment2.ipynb cell a6b7c8d9:
```python
authors_df = pd.read_csv('./final_authors.csv')
# authors_df has columns: cited_by_count, country_code, display_name, h_index, id, works_api_url, works_count
```

From Assignment2/Assignment2.ipynb cell q4-tfidf-code (Phase 3 output):
```python
top9_communities = community_df['community'].value_counts().head(9).index.tolist()
tf_community  # dict: community_int -> {word: tf_score}
tf_idf_community  # dict: community_int -> {word: tfidf_score}
community_df  # DataFrame: author_id, community, degree
```

From Week8/part1.ipynb cell a305eee6 (word cloud pattern to adapt):
```python
wordcloud = WordCloud(width=800, height=400).generate_from_frequencies(tf_community[comm])
plt.figure(figsize=(10, 5))
plt.imshow(wordcloud, interpolation='bilinear')
plt.axis('off')
plt.title(f'Community {comm} Word Cloud\nTop Authors: {author_ids}')
plt.show()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append TF-IDF explanation markdown cell (Ex1 Q1)</name>
  <read_first>
    - Assignment2/Assignment2.ipynb (read full notebook to find last cell id for append position)
    - .planning/phases/04-part-2-writing-and-word-clouds/04-CONTEXT.md (locked decisions)
  </read_first>
  <files>Assignment2/Assignment2.ipynb</files>
  <action>
Append one markdown cell after the last existing cell (q4-analysis). The cell must have id "ex1-q1-tfidf-explanation".

Cell content (use exactly this text — it is 133 words, within the 120-150 range):

```
## Exercise 1

### Q1: Explain TF-IDF in your own words. What is TF? What is IDF?

TF-IDF (Term Frequency–Inverse Document Frequency) is a numerical measure that captures how informative a word is for a specific document within a corpus. It is the product of two components.

**TF (Term Frequency)** measures how often a word appears in a given document relative to its total word count. A word appearing frequently in a community's corpus gets a high TF score. We use raw relative frequency (count / total tokens) rather than a log-transformed version.

**IDF (Inverse Document Frequency)** penalises words that appear across many documents. It is computed as log(N / df), where N is the total number of documents and df is the number of documents containing the word. We use the natural logarithm (base e); the base choice is a scaling constant that does not affect word rankings within a community.

Multiplying TF by IDF yields a score that is high only when a word is both locally frequent and globally rare — making TF-IDF far more discriminative than raw term frequency alone.
```

Append this cell to the notebook's `cells` array as a new cell object:
```json
{
  "cell_type": "markdown",
  "id": "ex1-q1-tfidf-explanation",
  "metadata": {},
  "source": ["## Exercise 1\n", "\n", "### Q1: Explain TF-IDF in your own words. What is TF? What is IDF?\n", "\n", "TF-IDF (Term Frequency–Inverse Document Frequency) is a numerical measure that captures how informative a word is for a specific document within a corpus. It is the product of two components.\n", "\n", "**TF (Term Frequency)** measures how often a word appears in a given document relative to its total word count. A word appearing frequently in a community's corpus gets a high TF score. We use raw relative frequency (count / total tokens) rather than a log-transformed version.\n", "\n", "**IDF (Inverse Document Frequency)** penalises words that appear across many documents. It is computed as log(N / df), where N is the total number of documents and df is the number of documents containing the word. We use the natural logarithm (base e); the base choice is a scaling constant that does not affect word rankings within a community.\n", "\n", "Multiplying TF by IDF yields a score that is high only when a word is both locally frequent and globally rare — making TF-IDF far more discriminative than raw term frequency alone."]
}
```
  </action>
  <verify>
    <automated>cd "/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci" && python3 -c "import json; nb=json.load(open('Assignment2/Assignment2.ipynb')); ids=[c.get('id','') for c in nb['cells']]; assert 'ex1-q1-tfidf-explanation' in ids, 'cell not found'; print('OK:', ids.index('ex1-q1-tfidf-explanation'))"</automated>
  </verify>
  <acceptance_criteria>
    - Cell id "ex1-q1-tfidf-explanation" exists in Assignment2.ipynb
    - Cell type is "markdown"
    - Source contains "TF (Term Frequency)" and "IDF (Inverse Document Frequency)"
    - Source contains "natural logarithm" or "base e"
    - Word count of source is between 120 and 150 words
  </acceptance_criteria>
  <done>Markdown cell with 120-150 word TF-IDF explanation appended, both TF and IDF defined, natural log mentioned</done>
</task>

<task type="auto">
  <name>Task 2: Append word cloud code cell (Ex2 VIZN-01)</name>
  <read_first>
    - Assignment2/Assignment2.ipynb (verify ex1-q1-tfidf-explanation was appended, find current last cell)
    - Assignment2/final_authors.csv (first 5 rows — confirm display_name and id column names)
    - Week8/part1.ipynb (cell a305eee6 — adapt word cloud pattern)
  </read_first>
  <files>Assignment2/Assignment2.ipynb</files>
  <action>
Append two cells after "ex1-q1-tfidf-explanation":

**Cell 1 — markdown header, id "ex2-wordcloud-header":**
```json
{
  "cell_type": "markdown",
  "id": "ex2-wordcloud-header",
  "metadata": {},
  "source": ["## Exercise 2\n", "\n", "### Generate a word cloud for each community. Display the top 3 authors' names alongside each cloud."]
}
```

**Cell 2 — code, id "ex2-wordcloud-code":**
```python
# Ex2: Word clouds for top 9 communities with real author names
# Resolve OpenAlex IDs to display names using final_authors.csv
name_map = dict(zip(authors_df['id'], authors_df['display_name']))

top9_communities = community_df['community'].value_counts().head(9).index.tolist()

fig, axes = plt.subplots(3, 3, figsize=(18, 12))
axes_flat = axes.flatten()

for idx, comm in enumerate(top9_communities):
    # Generate word cloud from TF frequencies
    wordcloud = WordCloud(width=600, height=300, background_color='white',
                          colormap='viridis').generate_from_frequencies(tf_community[comm])

    # Resolve top 3 authors by degree to display names
    top_authors_rows = (
        community_df[community_df['community'] == comm]
        .sort_values(by='degree', ascending=False)
        .head(3)
    )
    author_names = [name_map.get(aid, aid) for aid in top_authors_rows['author_id'].tolist()]

    ax = axes_flat[idx]
    ax.imshow(wordcloud, interpolation='bilinear')
    ax.axis('off')
    ax.set_title(
        f'Community {comm} Word Cloud\nTop Authors: {", ".join(author_names)}',
        fontsize=9, pad=6
    )

plt.suptitle('Word Clouds for Top 9 Communities (by Author Count)', fontsize=13, y=1.01)
plt.tight_layout()
plt.show()
```

Append as a code cell:
```json
{
  "cell_type": "code",
  "execution_count": null,
  "id": "ex2-wordcloud-code",
  "metadata": {},
  "outputs": [],
  "source": ["# Ex2: Word clouds for top 9 communities with real author names\n", "# Resolve OpenAlex IDs to display names using final_authors.csv\n", "name_map = dict(zip(authors_df['id'], authors_df['display_name']))\n", "\n", "top9_communities = community_df['community'].value_counts().head(9).index.tolist()\n", "\n", "fig, axes = plt.subplots(3, 3, figsize=(18, 12))\n", "axes_flat = axes.flatten()\n", "\n", "for idx, comm in enumerate(top9_communities):\n", "    # Generate word cloud from TF frequencies\n", "    wordcloud = WordCloud(width=600, height=300, background_color='white',\n", "                          colormap='viridis').generate_from_frequencies(tf_community[comm])\n", "\n", "    # Resolve top 3 authors by degree to display names\n", "    top_authors_rows = (\n", "        community_df[community_df['community'] == comm]\n", "        .sort_values(by='degree', ascending=False)\n", "        .head(3)\n", "    )\n", "    author_names = [name_map.get(aid, aid) for aid in top_authors_rows['author_id'].tolist()]\n", "\n", "    ax = axes_flat[idx]\n", "    ax.imshow(wordcloud, interpolation='bilinear')\n", "    ax.axis('off')\n", "    ax.set_title(\n", "        f'Community {comm} Word Cloud\\nTop Authors: {\", \".join(author_names)}',\n", "        fontsize=9, pad=6\n", "    )\n", "\n", "plt.suptitle('Word Clouds for Top 9 Communities (by Author Count)', fontsize=13, y=1.01)\n", "plt.tight_layout()\n", "plt.show()\n"]
}
```
  </action>
  <verify>
    <automated>cd "/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci" && python3 -c "import json; nb=json.load(open('Assignment2/Assignment2.ipynb')); ids=[c.get('id','') for c in nb['cells']]; assert 'ex2-wordcloud-header' in ids; assert 'ex2-wordcloud-code' in ids; src=''.join(nb['cells'][ids.index('ex2-wordcloud-code')]['source']); assert 'name_map' in src; assert 'display_name' in src; assert 'generate_from_frequencies' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Cell id "ex2-wordcloud-header" exists (markdown)
    - Cell id "ex2-wordcloud-code" exists (code)
    - Code contains "name_map = dict(zip(authors_df['id'], authors_df['display_name']))"
    - Code uses "generate_from_frequencies(tf_community[comm])"
    - Code uses name_map.get() to resolve author IDs to display names
    - Code uses subplots(3, 3) to show all 9 communities in a grid
  </acceptance_criteria>
  <done>Word cloud code cell appended — generates 3x3 grid, each panel titled with community number and resolved author display names</done>
</task>

</tasks>

<verification>
Run both automated verify commands. Both must print "OK". Additionally confirm cell order in notebook: q4-analysis → ex1-q1-tfidf-explanation → ex2-wordcloud-header → ex2-wordcloud-code.
</verification>

<success_criteria>
- Assignment2.ipynb contains cell "ex1-q1-tfidf-explanation" with 120-150 word TF-IDF explanation defining both TF and IDF and mentioning natural log
- Assignment2.ipynb contains cell "ex2-wordcloud-code" using name_map to resolve author IDs and generate_from_frequencies to build clouds
- Code is syntactically valid Python (no import errors when read)
</success_criteria>

<output>
After completion, create `.planning/phases/04-part-2-writing-and-word-clouds/04-01-SUMMARY.md`
</output>
