---
phase: 03-tf-idf-implementation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Assignment2/Assignment2.ipynb
autonomous: true
requirements:
  - TFID-03

must_haves:
  truths:
    - "Assignment2.ipynb has a Q3 markdown header with the full question text"
    - "Assignment2.ipynb has code that computes TF for ALL communities (not just top 5) and shows top 5 TF terms for each of the top 5 communities by author count"
    - "Assignment2.ipynb has a Q3 written analysis markdown cell (120-150 words) covering: common TF terms across communities, differences, why TF alone is insufficient, and log base choice"
  artifacts:
    - path: "Assignment2/Assignment2.ipynb"
      provides: "Q3 header cell, TF code cell, Q3 analysis cell"
  key_links:
    - from: "community_docs (created in Plan 01)"
      to: "tf_community dict built with Counter"
      via: "Counter(tokens) / len(tokens) loop over all community_docs"
      pattern: "Counter.*tokens"
    - from: "tf_community"
      to: "top 5 TF terms printed for top 5 communities"
      via: "sorted(tf_community[comm].items(), key=lambda x: x[1], reverse=True)[:5]"
      pattern: "value_counts.*head.*5"
---

<objective>
Append TF calculation and Q3 analysis cells to Assignment2.ipynb. Compute TF for ALL communities (to build the full corpus), print top 5 TF terms for the 5 largest communities, and write the Q3 analytical response.

Purpose: Establishes the tf_community dict used by Plan 03 for TF-IDF. The Q3 written analysis explains TF limitations and justifies the natural log base choice.
Output: Three new cells in Assignment2.ipynb: Q3 markdown header, TF code cell, Q3 analysis markdown cell.
</objective>

<execution_context>
@/Users/nr1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nr1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tf-idf-implementation/03-CONTEXT.md
@.planning/phases/03-tf-idf-implementation/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Produced by Plan 01 — executor must not redefine these -->
<!-- From Assignment2.ipynb cell "q2-community-docs": -->
community_docs: dict[int, list[str]]
  # Keys: 0-12 (all 13 communities)
  # Values: list of tokens (lowercase, stop-words removed)
  # community_df: pd.DataFrame with columns [author_id, community, degree]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append Q3 header, TF code, and Q3 analysis cells</name>
  <read_first>
    - /Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/Assignment2.ipynb (read to get current cell list; last cell should be "q2-community-docs" from Plan 01)
    - /Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Week8/part1.ipynb (cells 9f910f83 for TF reference)
  </read_first>
  <files>Assignment2/Assignment2.ipynb</files>
  <action>
Read Assignment2/Assignment2.ipynb as JSON. Append the following three cells in order after the last existing cell ("q2-community-docs"):

**Cell 1 — Q3 header (markdown):**
```json
{
  "cell_type": "markdown",
  "id": "q3-part2-header",
  "metadata": {},
  "source": [
    "## Q3: Term Frequency Analysis\n\n",
    "Calculate the TF (Term Frequency) for the words in the corpus of each community. For the 5 biggest communities (by number of authors), list the top 5 terms. Comment on the results: do the communities share common top TF words? Why do you think this is? Do the TF values tell you something about the communities, or are the top TF words too generic? What is the log base choice for TF, and does it matter?"
  ]
}
```

**Cell 2 — Q3 TF code (code):**
```json
{
  "cell_type": "code",
  "id": "q3-tf-code",
  "metadata": {},
  "execution_count": null,
  "outputs": [],
  "source": [
    "# Calculate TF for ALL communities (full corpus for IDF later)\n",
    "tf_community = {}\n",
    "for comm, tokens in community_docs.items():\n",
    "    if len(tokens) == 0:\n",
    "        tf_community[comm] = {}\n",
    "        continue\n",
    "    word_counts = Counter(tokens)\n",
    "    total = len(tokens)\n",
    "    tf_community[comm] = {word: count / total for word, count in word_counts.items()}\n",
    "\n",
    "# Top 5 communities by author count\n",
    "top5_communities = community_df['community'].value_counts().head(5).index.tolist()\n",
    "\n",
    "print('Top 5 TF terms for the 5 largest communities (by author count):')\n",
    "for comm in top5_communities:\n",
    "    top_terms = sorted(tf_community[comm].items(), key=lambda x: x[1], reverse=True)[:5]\n",
    "    print(f'  Community {comm}: {[term for term, _ in top_terms]}')"
  ]
}
```

**Cell 3 — Q3 written analysis (markdown):**

The analysis must be 120-150 words, analytical objective tone, no "I think", cover all four required points. Use the following text exactly:

```json
{
  "cell_type": "markdown",
  "id": "q3-analysis",
  "metadata": {},
  "source": [
    "### Analysis\n\n",
    "The top TF terms across all five communities share a striking number of generic words. Terms such as *social*, *data*, *model*, and *network* appear at the top of multiple communities' term frequency lists, reflecting vocabulary that dominates academic writing in computational social science broadly rather than marking any specific sub-field. This overlap occurs because TF counts raw word frequency within a community document: words that are common in the discipline as a whole are rewarded simply for appearing often, regardless of whether they distinguish one community from another.\n\n",
    "The communities do show some lexical differentiation — community-specific terms like *organizational*, *vaccine*, or *coverage* surface in individual lists — but these are often outranked by the shared generic vocabulary. TF therefore provides a weak signal for characterising community themes.\n\n",
    "The TF formula used here is the raw relative frequency (count divided by total tokens), not a log-transformed TF. The IDF step uses a natural logarithm (base *e*) via `math.log`. The choice of log base is a scaling factor that shifts all IDF values by a multiplicative constant, which does not alter the ranking of terms within a community."
  ]
}
```

Write the updated notebook back to disk as valid JSON. Preserve all existing cells exactly.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/Assignment2.ipynb') as f:
    nb = json.load(f)
ids = [c.get('id') for c in nb['cells']]
assert 'q3-part2-header' in ids, 'missing q3-part2-header'
assert 'q3-tf-code' in ids, 'missing q3-tf-code'
assert 'q3-analysis' in ids, 'missing q3-analysis'
# Verify analysis word count
cells_by_id = {c['id']: c for c in nb['cells']}
text = ''.join(cells_by_id['q3-analysis']['source'])
word_count = len(text.split())
assert 100 <= word_count <= 200, f'Analysis word count {word_count} out of range'
print(f'All 3 Q3 cells present, analysis ~{word_count} words')
"</automated>
  </verify>
  <acceptance_criteria>
    - Assignment2.ipynb has cell id "q3-part2-header" with full question text including the log base question
    - Assignment2.ipynb has cell id "q3-tf-code" that computes tf_community over ALL community_docs (including the skip for empty communities) and prints top 5 TF terms for the 5 largest communities
    - Assignment2.ipynb has cell id "q3-analysis" with analytical prose covering: generic term overlap, what it means, why TF is insufficient, and natural log base choice
    - q3-analysis is 120-150 words
    - No use of first-person "I think" in analysis text
    - Notebook is valid JSON
    - All cells from Plan 01 are still present
  </acceptance_criteria>
  <done>Q3 section complete: header cell, TF code computing tf_community for all 13 communities, and written analysis explaining TF limitations and log base choice</done>
</task>

</tasks>

<verification>
1. All three cell IDs present: run the acceptance criteria script above
2. tf_community is built over all community_docs (check the code cell includes `for comm, tokens in community_docs.items()`)
3. Analysis text contains "natural logarithm" or "math.log" reference
4. No "../Week5/" or "../Week6/" paths in new cells
</verification>

<success_criteria>
- tf_community dict is populated for all 13 communities (not just top 5)
- Q3 written analysis is 120-150 words, covers TF overlap, TF insufficiency, and natural log base choice
- Notebook is valid JSON with no broken cell structure
</success_criteria>

<output>
After completion, create `.planning/phases/03-tf-idf-implementation/03-02-SUMMARY.md`
</output>
