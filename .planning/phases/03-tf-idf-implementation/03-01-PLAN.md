---
phase: 03-tf-idf-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assignment2/Assignment2.ipynb
  - Assignment2/author_communities.csv
autonomous: true
requirements:
  - TFID-02

must_haves:
  truths:
    - "author_communities.csv exists at Assignment2/author_communities.csv"
    - "Assignment2.ipynb has a Part 2 section header markdown cell after the Part 1 reflection cells"
    - "Assignment2.ipynb has an imports cell containing nltk, string, Counter, math, ast, word_tokenize, stopwords, WordCloud, matplotlib"
    - "Assignment2.ipynb has code that loads community_df from ./author_communities.csv using pandas"
    - "Assignment2.ipynb has the inverted_index_to_text() function and tokenize() function defined"
    - "Assignment2.ipynb has code that creates community_docs via groupby/apply using pandas explode and groupby"
    - "community_docs has entries for all 13 communities and the cell prints token counts for each"
  artifacts:
    - path: "Assignment2/author_communities.csv"
      provides: "Community assignments for all 466 authors"
    - path: "Assignment2/Assignment2.ipynb"
      provides: "New cells: Part 2 header, imports, data loading, community doc creation"
  key_links:
    - from: "Assignment2/author_communities.csv"
      to: "community_df = pd.read_csv('./author_communities.csv')"
      via: "relative path in notebook"
      pattern: "read_csv.*author_communities"
    - from: "Assignment2/D2_temp_papers.csv"
      to: "abstracts_df merged with community_df"
      via: "explode + merge on author_ids"
      pattern: "explode.*author_ids"
---

<objective>
Copy the community assignments data file into Assignment2/, then append the Part 2 foundation cells to Assignment2.ipynb: section header, imports, data loading, helper functions, and community document creation via pandas groupby/explode.

Purpose: All subsequent Phase 3 plans depend on community_docs being built correctly from all 13 communities. This plan establishes the data pipeline before TF and TF-IDF calculations.
Output: author_communities.csv in Assignment2/; five new cells appended to Assignment2.ipynb covering the Part 2 setup through community document creation.
</objective>

<execution_context>
@/Users/nr1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nr1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tf-idf-implementation/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy author_communities.csv into Assignment2/</name>
  <read_first>
    - /Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Week6/author_communities.csv (source file to copy)
  </read_first>
  <files>Assignment2/author_communities.csv</files>
  <action>
Copy the file from Week6/author_communities.csv to Assignment2/author_communities.csv using the Bash tool:

```bash
cp "/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Week6/author_communities.csv" \
   "/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/author_communities.csv"
```

The file has columns: author_id, community, degree — 466 rows. Do not modify the file content.
  </action>
  <verify>
    <automated>ls "/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/author_communities.csv" && wc -l "/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/author_communities.csv"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at Assignment2/author_communities.csv
    - File has 467 lines (466 data rows + 1 header)
  </acceptance_criteria>
  <done>author_communities.csv exists in Assignment2/ with 466 data rows</done>
</task>

<task type="auto">
  <name>Task 2: Append Part 2 foundation cells to Assignment2.ipynb</name>
  <read_first>
    - /Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/Assignment2.ipynb (read full file to get current cell list and last cell id)
    - /Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Week8/part1.ipynb (source code reference for functions)
  </read_first>
  <files>Assignment2/Assignment2.ipynb</files>
  <action>
Read Assignment2/Assignment2.ipynb as JSON. The last cell currently is id "q9-reflection". Append the following new cells to the "cells" array in order:

**Cell 1 — Part 2 section header (markdown):**
```json
{
  "cell_type": "markdown",
  "id": "part2-header",
  "metadata": {},
  "source": ["## Part 2: TF-IDF Analysis"]
}
```

**Cell 2 — Imports (code):**
```json
{
  "cell_type": "code",
  "id": "part2-imports",
  "metadata": {},
  "execution_count": null,
  "outputs": [],
  "source": [
    "import nltk\n",
    "import ast\n",
    "import string\n",
    "import math\n",
    "from collections import Counter\n",
    "from nltk.tokenize import word_tokenize\n",
    "from nltk.corpus import stopwords\n",
    "from wordcloud import WordCloud\n",
    "import matplotlib.pyplot as plt\n",
    "import pandas as pd\n",
    "\n",
    "nltk.download('punkt_tab')\n",
    "nltk.download('stopwords')"
  ]
}
```

**Cell 3 — Load community_df and abstracts, build merged (code):**
```json
{
  "cell_type": "code",
  "id": "part2-load-data",
  "metadata": {},
  "execution_count": null,
  "outputs": [],
  "source": [
    "# Load community assignments\n",
    "community_df = pd.read_csv('./author_communities.csv')\n",
    "\n",
    "# Load papers with abstracts\n",
    "abstracts_df = pd.read_csv('./D2_temp_papers.csv')\n",
    "abstracts_df['author_ids'] = abstracts_df['author_ids'].apply(ast.literal_eval)\n",
    "\n",
    "# Explode so each row has one author\n",
    "exploded = abstracts_df.explode('author_ids')\n",
    "exploded['author_ids'] = exploded['author_ids'].str.strip()\n",
    "\n",
    "# Merge with community assignments\n",
    "merged = exploded.merge(community_df, left_on='author_ids', right_on='author_id')\n",
    "\n",
    "# Helper: convert abstract inverted index to plain text\n",
    "def inverted_index_to_text(inv_idx_str):\n",
    "    if pd.isna(inv_idx_str):\n",
    "        return ''\n",
    "    inv_idx = ast.literal_eval(inv_idx_str)\n",
    "    words = [''] * (max(max(pos) for pos in inv_idx.values()) + 1)\n",
    "    for word, positions in inv_idx.items():\n",
    "        for pos in positions:\n",
    "            words[pos] = word\n",
    "    return ' '.join(words)\n",
    "\n",
    "# Convert abstracts to text\n",
    "merged['text'] = merged['abstract_inverted_index'].apply(inverted_index_to_text)"
  ]
}
```

**Cell 4 — Q2 header (markdown):**
```json
{
  "cell_type": "markdown",
  "id": "q2-part2-header",
  "metadata": {},
  "source": [
    "## Q2: Create Community Token Documents\n\n",
    "For each community, create a large token document by combining all paper abstracts written by authors in that community. Use pandas `groupby` and `apply` to aggregate tokens per community."
  ]
}
```

**Cell 5 — Q2 code: tokenize and create community_docs (code):**
```json
{
  "cell_type": "code",
  "id": "q2-community-docs",
  "metadata": {},
  "execution_count": null,
  "outputs": [],
  "source": [
    "stop_words = set(stopwords.words('english'))\n",
    "\n",
    "def tokenize(text):\n",
    "    tokens = word_tokenize(text.lower())\n",
    "    return [w for w in tokens if w not in stop_words and w not in string.punctuation]\n",
    "\n",
    "# Group by community — combine all tokens into one document per community\n",
    "# Uses pandas groupby + apply (explode already done above)\n",
    "community_docs = (\n",
    "    merged.groupby('community')['text']\n",
    "    .apply(lambda texts: tokenize(' '.join(texts)))\n",
    "    .to_dict()\n",
    ")\n",
    "\n",
    "for comm in sorted(community_docs.keys()):\n",
    "    print(f'Community {comm}: {len(community_docs[comm])} tokens')"
  ]
}
```

Write the updated notebook back to disk as valid JSON. Preserve all existing cells and their outputs exactly. Only append the five new cells above. Do NOT modify any existing cell.

Expected output when the Q2 code cell runs:
```
Community 0: 7318 tokens
Community 1: 742 tokens
Community 2: 907 tokens
Community 3: 21965 tokens
Community 4: 9660 tokens
Community 5: 950 tokens
Community 6: 25316 tokens
Community 7: 326 tokens
Community 8: 3838 tokens
Community 9: 5020 tokens
Community 10: 4611 tokens
Community 11: 2095 tokens
Community 12: 0 tokens
```
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('/Users/nr1/Documents/DTU 4. Semester/CompSci 02467/CompSci Gruppearbejde/CompSci/Assignment2/Assignment2.ipynb') as f:
    nb = json.load(f)
ids = [c.get('id') for c in nb['cells']]
assert 'part2-header' in ids, 'missing part2-header'
assert 'part2-imports' in ids, 'missing part2-imports'
assert 'part2-load-data' in ids, 'missing part2-load-data'
assert 'q2-part2-header' in ids, 'missing q2-part2-header'
assert 'q2-community-docs' in ids, 'missing q2-community-docs'
print('All 5 cells present')
"</automated>
  </verify>
  <acceptance_criteria>
    - Assignment2.ipynb has cell id "part2-header" (markdown, "## Part 2: TF-IDF Analysis")
    - Assignment2.ipynb has cell id "part2-imports" with nltk, string, Counter, math imports
    - Assignment2.ipynb has cell id "part2-load-data" loading from ./author_communities.csv and ./D2_temp_papers.csv
    - Assignment2.ipynb has cell id "q2-community-docs" using groupby/apply to create community_docs
    - All existing cells (including q9-reflection) are preserved with their outputs
    - Notebook is valid JSON (python3 -c "import json; json.load(open('Assignment2/Assignment2.ipynb'))" succeeds)
  </acceptance_criteria>
  <done>Five new Part 2 foundation cells are appended to Assignment2.ipynb; community_docs creation uses pandas groupby/apply over ./author_communities.csv</done>
</task>

</tasks>

<verification>
1. Assignment2/author_communities.csv exists: `ls Assignment2/author_communities.csv`
2. Notebook is valid JSON: `python3 -c "import json; json.load(open('Assignment2/Assignment2.ipynb'))"`
3. All 5 new cell IDs present: run the acceptance criteria script above
4. Data paths are relative (grep for '../' — should return nothing in new cells)
</verification>

<success_criteria>
- author_communities.csv exists at Assignment2/author_communities.csv
- Assignment2.ipynb contains Part 2 header, imports, data loader, inverted_index_to_text(), tokenize(), and community_docs groupby code
- All data paths use ./relative/paths not ../Week5/ or ../Week6/
- Notebook is valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/03-tf-idf-implementation/03-01-SUMMARY.md`
</output>
